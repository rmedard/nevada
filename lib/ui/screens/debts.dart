import 'package:flutter/material.dart';

class Debts extends StatelessWidget {
  const Debts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Debts'),);
  }
}
