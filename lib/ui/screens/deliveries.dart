import 'package:flutter/material.dart';

class Deliveries extends StatelessWidget {
  const Deliveries({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Livraisons'),);
  }
}
